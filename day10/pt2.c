#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>
#include <stdint.h>
#include <assert.h>

#include "../utilities/file_utils.h"

int main(void)
{
    // size_t size = 0;
    // char *buffer = read_text_file("input.txt", &size);

    // size_t rows = 0;
    // for (size_t i = 0; i < size; i++)
    // {
    //     if (buffer[i] == '\n')
    //         rows++;
    // }

    return 0;
}
